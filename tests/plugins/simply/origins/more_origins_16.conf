# Options: {"domains": ["webvisor.com", "yandex.com"]}

if ($http_origin !~ "^https://webv.+isor\.com$") {
    add_header X-Frame-Options SAMEORIGIN;
}
if ($http_origin !~ "^https://webv.iso.r\.com$") {
    add_header X-Frame-Options SAMEORIGIN;
}
